---
import Layout from "../../layouts/Layout.astro";
import { newsData } from "../../data/daily/index";
import { Image } from "astro:assets";
import newsImg from "../../assets/hinta/news.png";

// Group news by year-month and count items
const monthGroups = newsData.reduce(
  (acc, item) => {
    const monthKey = `${item.year}-${item.month}`;
    if (!acc[monthKey]) {
      acc[monthKey] = {
        year: item.year,
        month: item.month,
        count: 0,
        dates: new Set<string>(),
      };
    }
    acc[monthKey].count += item.projects.length;
    acc[monthKey].dates.add(item.date);
    return acc;
  },
  {} as Record<string, { year: string; month: string; count: number; dates: Set<string> }>
);

// Sort by year-month descending (newest first) and group by year
const sortedMonths = Object.entries(monthGroups)
  .sort(([a], [b]) => b.localeCompare(a))
  .map(([key, data]) => ({
    key,
    ...data,
    daysCount: data.dates.size,
  }));

// Group months by year
const monthsByYear = sortedMonths.reduce(
  (acc, month) => {
    if (!acc[month.year]) {
      acc[month.year] = [];
    }
    acc[month.year].push(month);
    return acc;
  },
  {} as Record<string, typeof sortedMonths>
);

// Get sorted years (newest first)
const sortedYears = Object.keys(monthsByYear).sort((a, b) => b.localeCompare(a));

const monthNames = [
  "January",
  "February",
  "March",
  "April",
  "May",
  "June",
  "July",
  "August",
  "September",
  "October",
  "November",
  "December",
];

const getMonthName = (monthNum: string) => {
  return monthNames[parseInt(monthNum) - 1];
};
---

<Layout id="daily">
  <div class="container mx-auto px-4 py-8 max-w-5xl">
    <!-- Stats Summary -->
    {
      /*<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
      <div class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-xl p-6 shadow-md">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm font-medium text-gray-600 mb-1">Total Months</p>
            <p class="text-3xl font-bold text-indigo-600">{sortedMonths.length}</p>
          </div>
          <div class="w-12 h-12 bg-indigo-100 rounded-full flex items-center justify-center">
            <svg
              class="w-6 h-6 text-indigo-600"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
              ></path>
            </svg>
          </div>
        </div>
      </div>

      <div class="bg-gradient-to-br from-purple-50 to-pink-50 rounded-xl p-6 shadow-md">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm font-medium text-gray-600 mb-1">Total Projects</p>
            <p class="text-3xl font-bold text-purple-600">
              {sortedMonths.reduce((sum, m) => sum + m.count, 0)}
            </p>
          </div>
          <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center">
            <svg
              class="w-6 h-6 text-purple-600"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"
              ></path>
            </svg>
          </div>
        </div>
      </div>

      <div class="bg-gradient-to-br from-green-50 to-teal-50 rounded-xl p-6 shadow-md">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm font-medium text-gray-600 mb-1">Active Days</p>
            <p class="text-3xl font-bold text-green-600">
              {sortedMonths.reduce((sum, m) => sum + m.daysCount, 0)}
            </p>
          </div>
          <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
            <svg
              class="w-6 h-6 text-green-600"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
      </div>
    </div>*/
    }

    <!-- Month Cards Grouped by Year -->
    {
      sortedYears.map((year) => (
        <div class="mb-12">
          <h2 class="flex flex-row flex-nowrap items-center my-10">
            <span class="flex-grow block border-t border-black" />
            <span class="flex-none block mx-4 px-4 py-2.5 text-3xl rounded leading-none font-medium bg-gradient-to-r from-indigo-600 to-purple-600 text-white">
              {year}
            </span>
            <span class="flex-grow block border-t border-black" />
          </h2>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {monthsByYear[year].map((month, index) => {
              const gradients = [
                "from-blue-500 to-indigo-600",
                "from-purple-500 to-pink-600",
                "from-green-500 to-teal-600",
                "from-orange-500 to-red-600",
                "from-cyan-500 to-blue-600",
                "from-violet-500 to-purple-600",
              ];
              const gradient = gradients[index % gradients.length];

              return (
                <a
                  href={`/daily/${month.key}?lang=en`}
                  class="group block bg-white rounded-xl shadow-md hover:shadow-2xl transition-all duration-300 overflow-hidden border border-gray-100 hover:border-indigo-200 hover:scale-105"
                >
                  <div class={`h-2 bg-gradient-to-r ${gradient}`} />
                  <div class="p-6">
                    <div class="flex items-start justify-between mb-4">
                      <div>
                        <h3 class="text-2xl font-bold text-gray-900 mb-1">
                          {getMonthName(month.month)}
                        </h3>
                        <p class="text-gray-500 text-lg">{month.year}</p>
                      </div>
                      <div
                        class={`w-12 h-12 bg-gradient-to-br ${gradient} rounded-lg flex items-center justify-center text-white font-bold text-lg shadow-md group-hover:scale-110 transition-transform`}
                      >
                        {month.month}
                      </div>
                    </div>

                    {/* 
                    <div class="space-y-2 mb-4">
                      <div class="flex items-center text-sm text-gray-600">
                        <svg
                          class="w-4 h-4 mr-2 text-indigo-500"
                          fill="none"
                          stroke="currentColor"
                          viewBox="0 0 24 24"
                        >
                          <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"
                          />
                        </svg>
                        <span>{month.count} projects</span>
                      </div>
                      <div class="flex items-center text-sm text-gray-600">
                        <svg
                          class="w-4 h-4 mr-2 text-green-500"
                          fill="none"
                          stroke="currentColor"
                          viewBox="0 0 24 24"
                        >
                          <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                          />
                        </svg>
                        <span>{month.daysCount} active days</span>
                      </div>
                    </div> */}
                    <div class="flex items-center text-indigo-600 font-medium text-sm group-hover:text-indigo-700">
                      View details
                      <svg
                        class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M9 5l7 7-7 7"
                        />
                      </svg>
                    </div>
                  </div>
                </a>
              );
            })}
          </div>
        </div>
      ))
    }
  </div>
</Layout>
